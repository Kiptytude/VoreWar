The files here are default versions that get copied to the UserData directory when run if the files don't already exist.  This allows them to not be overwritten when a new version is put to the same directory.